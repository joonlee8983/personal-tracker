# API URL - Use your computer's local IP address (not localhost)
# Find your IP with: ipconfig getifaddr en0 (macOS) or hostname -I (Linux)
# Example: http://192.168.1.100:3000
EXPO_PUBLIC_API_BASE_URL="http://YOUR_IP_ADDRESS:3000"

# For production (deployed to Vercel)
# EXPO_PUBLIC_API_BASE_URL="https://your-app.vercel.app"
